machine:
  node:
    version: 6.5.0

test:
  override:
    - "npm run lint"
    - "npm test -- --coverageDirectory $CIRCLE_ARTIFACTS/coverage":
        environment:
          JEST_JUNIT_OUTPUT: $CIRCLE_TEST_REPORTS/junit/test-results.xml

deployment:
  npm:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - npm publish
